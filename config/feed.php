<?php

/**
 * Configuration for feed parameters. Available configuration keys:
 * cache_dir: Directory to store cached files
 * max_cache_age: TTL for cached files in seconds. After this age, the cached feed will be reloaded.
 */

return [
    //
];
